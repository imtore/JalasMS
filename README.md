make a virtual environment with python version 3.7.5
install requirements with:
$pip install -r requirements.txt

don't forget to update requirements.txt if you've installed new packages
for this you can follow these instructions:

1. install pipreqs:
$pip install pipreqs

2. execute this command in top jalasMS directory:
$pipreqs